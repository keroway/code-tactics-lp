# Implementation Plans

Generated by the improve audit on 2026-09-03 against commit `61a60ba`.
Execute in the order below unless dependencies say otherwise. Each executor
must read its plan fully, honor STOP conditions, and update its row when done.

## Execution order & status

| Plan | Title                                 | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Raise the fast-uri override floor     | P1       | S      | —          | DONE   |
| 002  | Promote low-contrast slate-500 copy   | P2       | S      | —          | DONE   |
| 003  | Deduplicate the walkHtml gate helper  | P2       | S      | —          | DONE   |
| 004  | Cover mobile menu in the axe smoke    | P2       | S      | —          | DONE   |
| 005  | Correct content-plan's footer mapping | P3       | S      | —          | DONE   |
| 006  | Polish Japanese copy & docs wording   | P2       | M      | —          | DONE   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale — finding fixed independently or approach
abandoned).

## Dependency notes

All plans are independent. Plans 001–005 are done. Plan 006 touches copy in
src/ and documentation in docs/ and has no dependency on build/code fixes.

## Plan-to-finding mapping

- 001 — fast-uri `3.1.5` permits four high advisories patched by `>=3.1.6`.
- 002 — normal-size `text-slate-500` content is below WCAG AA contrast on the
  default `slate-950` background.
- 003 — two CI gate scripts duplicate the same `walkHtml` generator.
- 004 — the axe smoke never opens the mobile-only menu.
- 005 — the docs incorrectly map footer implementation to `Layout.astro`.
- 006 — screenshot alt/aria-label contradictions, stale HowItWorks footnote,
  "山を登れ" and internal release-notes tone in LP copy, and docs wording/links.

## Findings considered and rejected / deferred

- `extract-zip` high advisory: **rejected for now**. npm's latest is still
  `2.0.1`; advisory metadata names `>=2.0.2`, but that version is not
  published. Retain the documented `osv-scanner.toml` ignore until a fix ships.
- `qs` moderates (two): **deferred**. They are transitive under `@lhci/cli`
  only; compare a safe bounded update when a direct parent offers a compatible
  fix rather than forcing a broad override.
- Fixture tests for the two gate scripts: **deferred**. The scripts already
  run against real build output in CI; current duplication cleanup (003) is
  the smaller, higher-confidence maintenance win.
- Public-mode axe artifact scan: **deferred**. 004 intentionally verifies
  mobile behaviour in private mode only; test the `REPO_IS_PUBLIC=true` build
  later if the visibility gate changes.
- README's future "updates/news" link: **deferred direction item**. It is
  blocked by the main repository's public-release plan, not a defect in this
  LP.

## Execution notes (2026-09-03 run)

- 001/002/003/005 implemented and committed on branches
  `advisor/001-fast-uri-floor`, `advisor/002-contrast-slate-500`,
  `advisor/003-dedup-walk-html`, `advisor/005-content-plan-footer`
  respectively. Not pushed/PR'd (per plan git-workflow default).
- 002: left `ProgramSection.astro`'s other two `text-slate-500` usages
  (`{r.no}` index span, code-comment span) untouched — they are not among
  the four sites enumerated in the plan's "Current state" and are covered by
  the plan's own out-of-scope note.
- 004: two commits on branch `advisor/004-axe-mobile-menu`. First adds the
  mobile-viewport axe pass to `scripts/smoke-a11y.mjs` per plan (initially
  hit the plan's STOP condition — see below). Second is a follow-up fix
  (operator-approved) outside the plan's original scope: added
  `tabindex="0"` to the `<pre>` code block in `ProgramSection.astro` to
  resolve the `scrollable-region-focusable` (serious) violation the new
  mobile pass surfaced — the block is horizontally scrollable at 375px
  width and was not keyboard-focusable. `pnpm run smoke:a11y` now passes
  clean (38 rules, 0 violations) on the mobile pass.
